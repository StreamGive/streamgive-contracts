[package]
name = "donation-vault"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "27.0.6"
ngo-registry = { path = "../ngo-registry" }

[dev-dependencies]
soroban-sdk = { version = "27.0.6", features = ["testutils"] }
